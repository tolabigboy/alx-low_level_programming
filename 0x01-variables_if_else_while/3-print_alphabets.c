#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
*
* Return: Always 0 (success)
*/
int main(void)
{
	char alph;

	for (alph = 'a'; alpha <= 'z'; alph++)
		putchar(alph);
	for (alph = 'A'; alph <= 'Z'; alpa++)
		putchar(alph);
	putchar('\n')

	return (0);
}
