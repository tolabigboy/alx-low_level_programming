#include "main.h"
#include <stdio.h>
/**
 * mul - a function that multiplies two integers
 * @a: variable tested
 * @b: variable tested
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int sum = a * b;

	return (0);
}
